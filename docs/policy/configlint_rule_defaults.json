{
  "schema": "llm_scanner.configlint_org_policy.v1",
  "version": "2026-04-25",
  "description": "Default classification for configlint rule_id values. default_aggregate_escalates must match hf_bundle_scanner.dispatch.CONFIG_RISK_RULE_IDS (enforced by hf_bundle_scanner/tests/test_configlint_policy_template.py).",
  "code_reference": "hf_bundle_scanner.dispatch.CONFIG_RISK_RULE_IDS",
  "rules": [
    {
      "rule_id": "config_json_invalid",
      "default_aggregate_escalates": true,
      "signal_class": "integrity_parse",
      "notes": "Fix JSON before trusting other loader fields; blocks default bundle aggregate today."
    },
    {
      "rule_id": "trust_remote_code_enabled",
      "default_aggregate_escalates": true,
      "signal_class": "loader_remote_code",
      "notes": "Worst-case static loader posture in default CI; treat as security + legal, not a hyperparameter."
    },
    {
      "rule_id": "auto_map_custom_classes",
      "default_aggregate_escalates": true,
      "signal_class": "custom_class_mapping",
      "notes": "Unexpected class wiring / provenance story; blocking in default CI today."
    },
    {
      "rule_id": "use_auth_token_present",
      "default_aggregate_escalates": false,
      "signal_class": "secrets_hygiene",
      "notes": "Credential-in-config posture; informational in default CI today."
    },
    {
      "rule_id": "use_fast_tokenizer_truthy",
      "default_aggregate_escalates": false,
      "signal_class": "tokenizer_hygiene",
      "notes": "Legacy fast tokenizer path; audits/reproducibility, not RCE-by-itself."
    },
    {
      "rule_id": "use_safetensors_disabled",
      "default_aggregate_escalates": false,
      "signal_class": "weights_format_posture",
      "notes": "Explicit opt-out of safetensors; pairs with pickle-era weight risk in threat models."
    },
    {
      "rule_id": "local_files_only_false",
      "default_aggregate_escalates": false,
      "signal_class": "remote_fetch_posture",
      "notes": "Explicit opt-out of offline-only loads; supply-chain / mirror / egress story."
    },
    {
      "rule_id": "remote_pretrained_identifier_url",
      "default_aggregate_escalates": false,
      "signal_class": "remote_dependency_url",
      "notes": "Pretrained pointer is an http(s) URL; treat as a remote dependency with integrity controls."
    },
    {
      "rule_id": "tokenizer_subfolder_path_traversal",
      "default_aggregate_escalates": false,
      "signal_class": "path_integrity",
      "notes": "Tokenizer subfolder looks like traversal / absolute escape; packaging / extraction review."
    },
    {
      "rule_id": "http_proxies_configured",
      "default_aggregate_escalates": false,
      "signal_class": "egress_and_secrets",
      "notes": "Non-empty proxies object; watch for embedded credentials and egress policy drift."
    },
    {
      "rule_id": "torchscript_truthy",
      "default_aggregate_escalates": false,
      "signal_class": "tracing_surface",
      "notes": "TorchScript / tracing enabled; complicates audits and supply-chain review vs eager-only loads."
    }
  ]
}

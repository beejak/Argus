## graphify (LLM Scanner repo)

When answering architecture questions for this repository, prefer reading [`graphify-out/GRAPH_REPORT.md`](graphify-out/GRAPH_REPORT.md) if it exists.

After substantive edits to Python under `model-admission/` or `hf_bundle_scanner/`, run **`make graphify-update`** from the repo root when graphify is installed.

If you also use a parent workspace rule at `/root/CLAUDE.md`, treat that file as authoritative for paths outside this repo.
